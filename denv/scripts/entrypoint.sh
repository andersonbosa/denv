#!/usr/bin/env bash
# -*- coding: utf-8 -*-

### Use case: Can be used as `entrypoint` for SSH connections
# USER="ssh-user"
# BASTION_HOST="example.com.br"
# PORT=2222
# SOCKS_PORT=9090
# ​
# ssh ${USER}@${BASTION_HOST} -p ${PORT} -D 0.0.0.0:${SOCKS_PORT}

### Use case: most common, just a shell
/usr/bin/env bash
